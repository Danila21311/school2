services:
  - type: web
    name: liquid-school-portal
    env: node
    buildCommand: "npm install && npm run build"
    startCommand: "node server.js"
    envVars:
      - key: NODE_VERSION
        value: "20"
